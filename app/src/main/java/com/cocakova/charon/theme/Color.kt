package com.cocakova.charon.theme

import androidx.compose.ui.graphics.Color

// The river palette. Dark-first: Charon works at night.
val StyxBlack = Color(0xFF000000)   // OLED background — the far shore
val AbyssInk = Color(0xFF0A0F14)    // surfaces
val DepthSlate = Color(0xFF141C24)  // raised surfaces
val StyxTeal = Color(0xFF3ECFB2)    // primary — the water's glow
val DeepTeal = Color(0xFF1C5F53)    // dimmed water
val ObolGold = Color(0xFFD9A441)    // secondary — the ferryman's coin
val BoneWhite = Color(0xFFE6EDF3)   // primary text
val MistGrey = Color(0xFF8FA3AD)    // secondary text
val WarnEmber = Color(0xFFE0563E)   // errors / host-key mismatch red
